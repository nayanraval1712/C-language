#include <stdio.h>
void main()
{
    int month ;

    printf("insurt the number  ");
    scanf("%d", & month);

    if (month==2)
    {
        printf("if month is feb it has 28 or 29 day");
    }
    else
    {printf("sorry");}
    
}